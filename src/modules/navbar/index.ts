import * as Components from "./components";
import * as Containers from "./containers";
import * as Types from "./types";

export const NavbarModule = {
  Components,
  Containers,
  MainContainer: Containers.NavbarContainer,
  Types
};
